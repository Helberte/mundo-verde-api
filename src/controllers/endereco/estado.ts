import Controller from "@controllers/controller";
import { Request, Response } from "express";

export default new class Estado extends Controller {
  
  public async criarEstado(req: Request, res: Response): Promise<void> {
    
  }
}